playwright>=1.40,<2
# >=3.9 而不是 >=3.8：webui/server/_serve.py 用了 AppRunner(shutdown_timeout=...)，
# 该参数是 aiohttp 3.9.0 才加到 BaseRunner 的。3.8.x 会把它当成未知 kwarg 一路透传，
# 在 runner.setup() 抛 TypeError —— 而 main._start_webui 只捕获 OSError/PermissionError，
# 于是整个插件加载失败，不只是 WebUI 不可用。
aiohttp>=3.9,<4
jinja2>=3.0,<4
argon2-cffi>=23.1,<26
pyjwt>=2.8,<3
